"use client"

/** Financials tab body — entry point removed from event tabs (AUD-0073). */
export default function FinancialsTab() {
  return (
    <div className="rounded-lg border border-white/10 bg-white/5 px-4 py-8 text-center text-sm text-muted-foreground">
      Event financials are not available yet.
    </div>
  )
}
