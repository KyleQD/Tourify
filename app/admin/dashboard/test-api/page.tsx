import { notFound } from "next/navigation"

/**
 * Admin Dashboard Builder (orp-test-api): wont-fix — keep API smoke tests out of product admin.
 */
export default function TestApiDisabledPage() {
  notFound()
}
