# The current lockfile includes peer ranges that npm 10 resolves strictly even
# though the dependency tree is already pinned and used by the application.
# Keep CI/developer installs deterministic until those upstream peer ranges are
# reconciled in a dedicated dependency-upgrade pass.
legacy-peer-deps=true
