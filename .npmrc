# The committed package-lock.json contains a dependency tree produced with
# legacy peer resolution (notably @base-ui/react + date-fns 3 alongside
# react-day-picker 8). npm ci must use the same resolution mode to reproduce
# that lockfile deterministically instead of failing before verification runs.
legacy-peer-deps=true
