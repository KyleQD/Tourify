// Compatibility import path. The generated Supabase schema contract lives in
// lib/database.types.ts and is checked against a fresh local migration replay.
export type { Database } from "@/lib/database.types"
