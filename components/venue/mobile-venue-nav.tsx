/**
 * Compatibility export for consumers that still resolve the legacy shared
 * venue tree. The canonical implementation lives with venue operations.
 */
export { MobileVenueNav } from "@/app/venue/components/mobile-venue-nav"
