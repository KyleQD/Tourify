# Work gaps (WORK-001 audit)

Triage key: **missing** = does not exist / cannot be verified live; **incomplete** = exists but partial, duplicated, or not reconciled; **improve** = works but should be better. Every item has evidence + location. Counts: 8 missing / 9 incomplete / 6 improve.

## Missing

| # | Triage | Gap | Evidence | Location |
| --- | --- | --- | --- | --- |
| M1 | missing | **PII hardening migration not applied** — `staff_onboarding_sensitive_vault`, `can_view_hiring_pii`, and the applicant-own RLS rewrite exist only as a STAGED migration; the sensitive-field endpoint (`/api/hiring/onboarding/sensitive/[candidateId]`) and `onboarding_responses` PII policies cannot be verified live | WS-1.1 backlog item "NOT applied"; manifest `docs/engineering/migration-validation/20260823210000_harden_hiring_onboarding_pii.json` (status `planned`, no isolated/staging/production evidence); `20260823210000_harden_hiring_onboarding_pii.sql` | `docs/DEVELOPMENT_BACKLOG.md` §WS-1.1; migration row in `docs/engineering/generated/database-objects.md` |
| M2 | missing | **Worker check-in/out (and acknowledge) disabled by default** — gated on `FEATURE_WORK_MODE_WORKER_ACTIONS === "1"`; UI explicitly says "The reviewed worker-actions SQL must be applied and verified before check-in is enabled" | `lib/work-mode/read-model.ts` (`workerActionsAvailable`); `components/work-mode/work-mode-workspace.tsx` check-in branch; `20260823170000_worker_checkin_contract.sql` | `lib/work-mode/read-model.ts`, `components/work-mode/work-mode-workspace.tsx` |
| M3 | missing | **Staffing persona matrix absent** — WS-1.1 accept requires "persona-matrix covers staffing personas", but the only persona inventory is `docs/organization-personas.md` (General/Artist/Venue/Organization + org roles); no staffing-specific personas (worker, workforce manager, operations manager, finance manager, revoked member, cross-org, unauthenticated) are recorded | WS-1.1 accept text; `docs/organization-personas.md`; staffing personas referenced only in admin-audit remediation plan (`docs/admin-audit/ux/MASTER_REMEDIATION_PLAN.md`) | `docs/DEVELOPMENT_BACKLOG.md` §WS-1.1 |
| M4 | missing | **Legacy `.agents/` specialist ledgers absent** — the task scope named `.agents/` ledgers as evidence; no work/hiring ledgers exist there (glob returned no files); only `docs/work-packets/TA-PH0.md` documents this area | glob on `.agents/` (0 files); `docs/work-packets/TA-PH0.md` is the sole bounded work packet | repo root `.agents/` |
| M5 | missing | **No hiring audit route in the working set** — `hiring-audit-panel.tsx` component and `hiring_audit_events` table exist, but the audit data routes live under `/api/admin/applications/[id]/audit` and `/api/venue/hiring/audit`, not under `/api/hiring/**`; the panel's consumer is ambiguous within the work working set | `components/hiring/hiring-audit-panel.tsx`; `hiring_audit_events` (`20260330120000_hiring_audit_events.sql`); `app/api/admin/applications/[id]/audit/route.ts`, `app/api/venue/hiring/audit/route.ts` | `docs/engineering/generated/api-routes.md` |
| M6 | missing | **No aggregated pay/travel/contacts worker data** — Work Mode pay/travel/contacts views render only published `work_mode_publications` of matching types; there is no worker-scoped pay statement, itinerary, or contact API in the work working set (payroll lives in admin surface) | `components/work-mode/work-mode-workspace.tsx` pay/travel/contacts branches; `app/api/admin/workforce/payroll-exports` | `components/work-mode/work-mode-workspace.tsx` |
| M7 | missing | **No worker-facing shift conflict / availability signal** — schedule view says "Conflicts are not inferred"; no scheduling-conflict check for the worker surface (conflicts route lives under `/api/admin/workforce/conflicts`) | `components/work-mode/work-mode-workspace.tsx` schedule branch; `app/api/admin/workforce/conflicts` | `docs/engineering/generated/api-routes.md` |
| M8 | missing | **Check-in/out SQL not yet validated in any environment** — `worker_shift_check_in`/`worker_shift_check_out` from `20260823170000_worker_checkin_contract.sql` have no test coverage in `__tests__/work-mode/` or `__tests__/hiring/` (tests cover API telemetry and read model, not the RPC contract) | `__tests__/work-mode/work-mode-api.test.ts`; `20260823170000_worker_checkin_contract.sql` | `__tests__/work-mode/` |

## Incomplete

| # | Triage | Gap | Evidence | Location |
| --- | --- | --- | --- | --- |
| I1 | incomplete | **Three parallel hiring application surfaces** — `/api/hiring/applications` (org/venue polymorphic), `/api/admin/applications` + audit, `/api/venue/hiring/applications`, plus `/api/artist-jobs/applications` and legacy `/api/job-applications`; no single canonical apply/read/triage path for employers | `docs/engineering/generated/api-routes.md` rows for `/api/hiring/applications*`, `/api/admin/applications*`, `/api/venue/hiring/applications*`, `/api/job-applications`, `/api/artist-jobs/*/applications` | `docs/engineering/generated/api-routes.md` |
| I2 | incomplete | **Job posting lifecycle split across surfaces** — statuses `draft/published/paused/closed/filled/archived` in `lib/hiring/states.ts`, lifecycle actions in `lib/hiring/job-posting-lifecycle.ts` + `components/hiring/job-posting-lifecycle-actions.tsx`, wizard duplication (`admin-job-posting-wizard.tsx`, `artist-job-posting-wizard.tsx`, `job-posting-wizard-shell.tsx`), and scoping/seats RPCs (`has_perm`, `is_org_member`, `20260821180438_job_posting_scopes_and_organization_seats.sql`) — standardization test exists (`__tests__/jobs/job-posting-standardization.test.ts`) but surfaces are not consolidated | `lib/hiring/states.ts`, `components/hiring/*wizard*.tsx`, `components/job-posting/*wizard*.tsx`, `__tests__/jobs/job-posting-standardization.test.ts` | `components/hiring/`, `components/job-posting/` |
| I3 | incomplete | **Onboarding split across three stacks** — candidate endpoints (`/api/hiring/onboarding/**`), admin workflows/templates/role-packs (`/api/admin/onboarding/**`, ~19 routes), and legacy universal onboarding (`/api/onboarding/**`, `/api/onboarding-templates/**`, `/api/migrations/create-onboarding`, `/api/venue/staff-onboarding`); overlapping onboarding tables (`staff_onboarding_candidates`, `staff_onboarding`, `onboarding_responses`, `worker_onboarding_profiles`, `onboarding_templates`, `onboarding_flows`, `staff_onboarding_templates`) | `docs/engineering/generated/api-routes.md`; `docs/engineering/generated/database-objects.md` table rows | `docs/engineering/generated/` |
| I4 | incomplete | **Onboarding PII flow partially redundant** — `sensitive-field-utils.ts` redaction + `onboarding/sensitive/[candidateId]` + staged vault vs `staff_onboarding_sensitive_vault`; while the vault is unapplied, sensitive responses still flow through `onboarding_responses`/`staff_onboarding` permissive paths | `lib/hiring/sensitive-field-utils.ts`; `20260823210000_harden_hiring_onboarding_pii.sql` (staged); manifest postflight note | `lib/hiring/sensitive-field-utils.ts`; `docs/engineering/migration-validation/20260823210000_harden_hiring_onboarding_pii.json` |
| I5 | incomplete | **Employment assignment status model duplicated** — active statuses `invited/confirmed/active` in `lib/work-mode/read-model.ts` + `EmploymentAssignmentStatus` type + `employment_assignments` RLS/status RPCs, while `staff_shift_assignments` (compat migration) and legacy `event_crew_assignments` carry their own lifecycle; no single source of truth | `lib/work-mode/read-model.ts`; `types/hiring-roster-work-mode.ts`; `20260714015225_hiring_hub_roster_management_compat.sql`; `archive/enhanced_staff_management_schema.sql` | `lib/work-mode/read-model.ts` |
| I6 | incomplete | **Roster surface duplicated** — `hiring-roster-panel.tsx` + `team-roster-panel.tsx` + `staff-operations-*` + `workforce-ui.tsx` present roster data, backed by view `unified_staff_roster` and `/api/hiring/roster*` + `/api/admin/workforce/*`; no canonical roster component/contract | `components/hiring/hiring-roster-panel.tsx`, `team-roster-panel.tsx`, `workforce-ui.tsx`, `staff-operations-tabs.tsx`; `20260602120000_unified_staff_roster.sql` | `components/hiring/` |
| I7 | incomplete | **Guard-idiom standardization pending (WS-1.7)** — hiring/staffing mutations are confirmed guarded via ~8 idioms (entity/RBAC, canReviewStaffingApplications, resolveActingAdminContext, etc.), but the codified single wrapper is not done; many `/api/hiring/*` routes still show "manual review required" in the permission map, so per-route guard provenance is not machine-verifiable | `docs/DEVELOPMENT_BACKLOG.md` §WS-1.7; `docs/engineering/generated/permissions.md` hiring rows | `docs/engineering/generated/permissions.md` |
| I8 | incomplete | **Cross-domain fallout of WS-1.1 not mapped to work domain** — the venues/RBAC RLS baseline (`20260823210100_venues_rbac_rls_baseline.sql`) touches workforce/staffing tenants, and hiring PII hardening interacts with WS-0.9 private-docs; the work agent has no recorded dependency handoff for these | WS-1.1 staging rows; `docs/engineering/agents/database/GAPS.md`, `docs/engineering/agents/venue/GAPS.md` reference the same staged set | `docs/DEVELOPMENT_BACKLOG.md` §WS-1.1 |
| I9 | incomplete | **Workpacket acceptance criteria stale** — `docs/work-packets/TA-PH0.md` acceptance (presenter contract with lifecycle + tour/event scope fields; five-city E2E failing closed; seeded auth entry) is unchecked; work-packets ledger has no updated status | `docs/work-packets/TA-PH0.md` checklist (all unchecked) | `docs/work-packets/TA-PH0.md` |

## Improve

| # | Triage | Gap | Evidence | Location |
| --- | --- | --- | --- | --- |
| R1 | improve | **Working set boundary omits the worker API** — `app/work/**` and `components/work-mode/**` consume `app/api/work-mode/**`, `hooks/use-work-mode.ts`, `types/hiring-roster-work-mode.ts`, `lib/ux/client-telemetry`, but none are in `WORKING_SET.json`; future work tasks will miss their primary data path | `WORKING_SET.json` paths vs `hooks/use-work-mode.ts` imports; `app/api/work-mode/**` routes | `docs/engineering/agents/work/WORKING_SET.json` |
| R2 | improve | **Publication type strings are stringly-typed** — views filter on `site_map`, `day_sheet`, `task(s)`, `task_list`, `run_of_show`, `document(s)`, `credential`, `staff_document`, `travel`, `itinerary`, `lodging`, `transport`, `pay`, `payroll`, `compensation`, `contacts`, `crew_contacts`, `contact_sheet`; a type mismatch silently empties a view, and there is no shared constant/enum between publisher (admin) and reader (work) | `components/work-mode/work-mode-workspace.tsx` `publicationsFor(...)` calls; `work_mode_publications.publication_type` | `components/work-mode/work-mode-workspace.tsx` |
| R3 | improve | **Read-model fan-out is sequential and untested at integration level** — `getWorkModeAssignments` awaits assignments → shifts → publications → tasks/notifications in sequence (only the last two parallelized); no test covers the full payload contract (`WorkModeAssignmentsPayload`) | `lib/work-mode/read-model.ts`; `__tests__/work-mode/` coverage is API/telemetry only | `lib/work-mode/read-model.ts` |
| R4 | improve | **Hiring component barrel and duplicate shells** — `components/hiring/index.ts` barrel + `hiring-dashboard.tsx` vs `hiring-dashboard-shell.tsx` vs `hiring-overview-panel.tsx`/`staff-operations-overview.tsx` overlap; component inventory deserves consolidation before new surfaces are built | `components/hiring/index.ts`; `hiring-dashboard.tsx`, `hiring-dashboard-shell.tsx`, `staff-operations-overview.tsx` | `components/hiring/` |
| R5 | improve | **Status enums duplicated across layers** — hiring statuses in `lib/hiring/states.ts`, assignment statuses in `types/hiring-roster-work-mode.ts`, plus DB-level `canonical_application_status` RPC (`20260823180000_hiring_lifecycle.sql`); no generated-types parity check in place | `lib/hiring/states.ts`; `types/hiring-roster-work-mode.ts`; `canonical_application_status` row in `docs/engineering/generated/database-objects.md` | `types/hiring-roster-work-mode.ts`, `lib/hiring/states.ts` |
| R6 | improve | **Map staleness risk** — generated maps predate the task base SHA (`a7193116c…` vs `7cf660ad…`); repeated audits will need `npm run agents:generate` refresh note | `docs/engineering/generated/` SHA stamp; task `base_sha` | `docs/engineering/generated/` |

## Sequencing notes

- **M1 + M2 block live verification** of PII vault and worker check-in; both depend on the WS-1.1 gated pipeline (database agent owns the pipeline; work should hold a handoff dependency).
- **M3 (persona matrix)** is the WS-1.1 accept criterion owned jointly with QA; work can draft the staffing persona rows but the matrix registry lives outside the work working set.
- **I1/I2/I3/I6** are consolidation questions that need a product decision (canonical surface) before code is moved; see QUESTIONS.md.
- **R1** is a cheap, safe fix (working-set metadata only) that improves every future work task.