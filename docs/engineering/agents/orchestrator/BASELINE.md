# Orchestrator baseline

Audit date: 2026-09-09
Task: ORCH-001
Base SHA: 7cf660ad8422dbd3adbdb77369d94638cdc2231b

## What exists today

### Runtime implementation surface

The orchestrator is a documentation and coordination domain, not a runtime
product surface. It owns no web routes, React components, domain services, API
handlers, Supabase migrations, or database objects. Those surfaces are indexed
for routing work in `docs/engineering/generated/routes.md`,
`docs/engineering/generated/components.md`,
`docs/engineering/generated/api-routes.md`,
`docs/engineering/generated/database-schema.md`, and
`docs/engineering/generated/database-objects.md`; behavior remains owned and
verified by the corresponding domain agents.

The orchestrator's executable evidence is the control-plane tooling and its
records: the commands declared in `package.json`, their implementation under
`scripts/agent-tools/`, task records under `docs/engineering/tasks/`, handoffs
under `docs/engineering/handoffs/`, execution plans under
`docs/engineering/exec-plans/`, and validation output recorded in ORCH-001.
There are no orchestrator-local product tests; the focused acceptance gate is
`npm run agents:validate`, as specified by
`docs/engineering/agents/orchestrator/VERIFICATION.md` and the ORCH-001 task
record.

### Control plane (docs/engineering/)

- **INDEX.md** — required reading order, links to all core memory files. `docs/engineering/INDEX.md`
- **PROJECT_STATE.md** — bootstrap snapshot: dirty tree 225 modified / 161 untracked, baseline SHA, immediate queue. `docs/engineering/PROJECT_STATE.md`
- **SYSTEM_MAP.md** — durable area boundaries, 12 areas mapped, cross-cutting flows documented. `docs/engineering/SYSTEM_MAP.md`
- **DEPENDENCY_MAP.md** — runtime layers, principal dependencies (Next.js, Supabase, Stripe, Resend, Upstash, Sentry, Playwright). `docs/engineering/DEPENDENCY_MAP.md`
- **DECISIONS.md** — 5 cross-domain decisions (CP-001 through CP-004, with a duplicate CP-002 label). `docs/engineering/DECISIONS.md`

### Agent infrastructure

- **Registry** — 17 agents defined in `docs/engineering/agents/registry.yaml`: orchestrator, admin, artist, venue, organization, general-user, work, discover, music, marketplace, ticketing, social, database, design-system, integrations, qa, release.
- **Orchestrator domain** — 8 files: CHARTER.md, STATE.md, ARCHITECTURE.md, INTERFACES.md, VERIFICATION.md, BACKLOG.md, DECISIONS.md, WORKING_SET.json. All are bootstrap skeletons; none contain audit-derived content.
- **Agent identities** — referenced at `docs/engineering/agent-identities.md` (not yet read; pending expansion).
- **OpenCode agent team** — referenced at `.opencode/agent/README.md` (generated by `npm run agents:opencode:sync`).

### Generated maps

9 SHA-stamped topology files in `docs/engineering/generated/`:
- `routes.md`, `api-routes.md`, `components.md`, `database-schema.md`, `database-objects.md`, `permissions.md`, `integrations.md`, `project-map.md`
- README states: run `npm run agents:generate`, do not hand-edit.

### Task model

- Schema at `docs/engineering/tasks/task.schema.json`
- Index at `docs/engineering/tasks/TASK_INDEX.json`
- 17 active tasks (all `*-001.json` starter audits), 1 active non-starter (`ADMUX-0102.json`), 3 completed tasks.
- CLI: `npm run agents:task:create`, `npm run agents:checkpoint`, `npm run agents:context`.

### Workflow and verification

- `docs/DEVELOPMENT_WORKFLOW.md` — work packet template, 3 verification tiers (fast/feature/release), change-to-check map, completion report format, Docker local stack.
- `scripts/verify.mjs` — wraps `verify:fast`, `verify:feature`, `verify:release` commands.

### Existing work evidence

- `docs/work-packets/TA-PH0.md` — in-progress test entry path packet (auth test setup, hiring, tour verification).
- `docs/work-packets/TEMPLATE.md` — packet template.
- `.agents/` — 6 legacy specialist ledgers: admin-dashboard-builder, admin-feature-spec-builder, admin-ui-wiring, organization-ticketing, venue-pages-builder, plus flows/ and plans/ directories.
- `docs/AUDIT_FINDINGS_2026-08-23.md` — original platform audit (source of DEVELOPMENT_BACKLOG.md).
- `docs/DEVELOPMENT_BACKLOG.md` — 4-phase backlog (P0-P3), 10 launch gates, 5 fix passes documented.

### Control-plane npm scripts

- `npm run agents:generate` — refresh topology maps
- `npm run agents:validate` — validate control plane
- `npm run agents:task:create` — create task records
- `npm run agents:context` — build size-limited context packet
- `npm run agents:checkpoint` — checkpoint task progress
- `npm run agents:opencode:sync` — sync opencode agent definitions

## Intended direction

Per `docs/DEVELOPMENT_BACKLOG.md` and `docs/engineering/agents/orchestrator/BACKLOG.md`:

1. Complete bootstrap: generate maps, validate control plane, assign bounded tasks.
2. Resolve dirty-worktree ownership before broad refactors.
3. Route bounded work through the task/handoff/exec-plan model.
4. Maintain cross-domain dependency graph currency.
5. Coordinate the 17 domain agents through their starter audit tasks.
6. Drive the 4-phase backlog from P0 security fixes through P3 scale work.

Detailed human-facing work remains in `docs/work-packets/`, while canonical
task status remains in `docs/engineering/tasks/` per
`docs/DEVELOPMENT_WORKFLOW.md` and `docs/engineering/tasks/README.md`.
Cross-domain execution and ownership transfer use
`docs/engineering/exec-plans/README.md` and
`docs/engineering/handoffs/README.md`.

## Closeout verification — 2026-09-20

- The original 2026-09-09 snapshot above is retained as audit evidence rather
  than rewritten to resemble current topology.
- The bounded context packet was rebuilt with
  `npm run agents:context -- --task ORCH-001` at
  `ea5c36a3b468afb82d83809746d01ad479d38541`.
- The current control plane contains 17 registered agents, 113 task records,
  eight generated topology maps plus their README, an active execution-plan
  model, and pending/completed handoff records. Evidence paths are
  `docs/engineering/agents/registry.yaml`, `docs/engineering/tasks/`,
  `docs/engineering/generated/`, `docs/engineering/exec-plans/`, and
  `docs/engineering/handoffs/`.
- ORCH-002 is the follow-up task for current workspace curation and launch-path
  orchestration; ORCH-001 remains only the completed baseline audit.
