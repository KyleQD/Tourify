# Admin state

- Last reviewed SHA: `a7193116c5a677b1c2939aa4a66e9415dac6eed1`
- Last reviewed at: 2026-09-13
- Active task: `ADMIN-002` / `ADMIN-003` local-readiness implementation checkpoint
- Confidence: platform/org gate split and standard wrappers are implemented; legacy route migration remains open.

## Durable facts

- Mission: Own admin dashboards, admin operations, authorization gates, and audit registries.
- Default working set is recorded in `WORKING_SET.json`.
- Admin area inventory: 79 page routes, 289 API routes, 258 components, 281 lib files, 244 tests, 31 auth core files.
- Legacy ledgers (admin-dashboard-builder, admin-feature-spec-builder, admin-ui-wiring) are all COMPLETE or near-COMPLETE; reference them from task records instead of re-executing.
- Two P0 security items from DEVELOPMENT_BACKLOG.md: WS-0.8 (admin gate split) and WS-1.7 (admin guard sweep) must be resolved before launch.
- `ADM-B01` remains blocked on independent CI/staging evidence and must not be closed from local-only results.
- The W01 first-level Admin IA now uses six task-oriented groups, keeps canonical sidebar destinations reachable, and exposes matching destinations directly during search.
- Local verification does not promote canonical `ADM-B13` or `ADMUX-0101` status; those remain governed by the audit registry, batch dependencies, and immutable evidence rules.
- Generated maps are stale (SHA `a7193116`, 386/399 entries dirty) and should be refreshed when maps are out of date.
- Platform profile authority is now `profiles.is_admin` or a nonzero/recognized `admin_level`; `role = 'admin'` alone is not a platform grant. Organization authority stays in the acting-org capability context.
- `withPlatformAdmin` is the platform-only HTTP wrapper; `withAdminCapability` is the standard organization-scoped guard. The audit reader now uses the latter.
- Event analytics and event export now use `withAdminCapability('event.view')` and retain `assertAdminEventAccess` ownership checks; this is the first bounded legacy-route convergence batch.
- The generic admin task route now uses `withAdminCapability('logistics.view')` for reads and `withAdminCapability('logistics.manage')` for updates; `resolveAdminWorkspaceScope`, `requireOpsOrgId`, and `org_id` predicates remain the data-boundary checks.
- Site-map Work Mode publication now uses `withAdminCapability('site_map.edit')` and passes `admin.orgId` into `getSiteMapAccess`; the existing site-map `edit` access check remains required.
- Task-equipment attachment and removal now use `withAdminCapability('logistics.manage')`; parent organization resolution and unresolved-parent rejection remain enforced by `withLogisticsParentOrgId`.
- The admin staff route now uses `workforce.view` for reads and `workforce.manage` for mutations; its active workspace, org/event membership, and entity filters remain enforced.
- The analytics CSV export now uses `audit.view` and binds its financial/event queries to the acting organization (`admin.orgId`), matching the method-level route contract.
- Tour lifecycle transition POST now uses the standard `withAdminCapability` wrapper with a command-derived capability (`tour.manage`, `tour.publish`, `finance.approve`, or `tour.archive`); the wrapper enforces acting-organization and collaborator tour scope before `executeTourTransition`, while command-specific capabilities, lifecycle state checks, ownership, and idempotency remain enforced by the transition service.
- Tour admin grants now use `withAdminCapability('tour.manage')` while retaining the route’s `assertTourAuthority` resource check and explicit tour/org membership behavior.
- Organization communications settings GET now uses `org.settings.manage` and binds reads to `admin.orgId`; PATCH already used the same capability.
- Effective admin capabilities projection now uses `withAdminCapability('tour.view')`; it remains a server-only reflection of the acting organization’s resolved capabilities.
- Admin notifications GET/PATCH now use `withAdminCapability('communications.send')` while retaining workspace org/event filters; POST remains on the legacy guard because it has distinct user-targeting semantics.
- RBAC role assignment and entity assignments/audit reads now use `org.roles.manage`; existing `has_entity_permission(..., 'MANAGE_MEMBERS')` checks remain the entity resource boundary.
- Logistics task update/delete, single status transition, and bulk status transition handlers now use `withAdminCapability('logistics.manage')`; the canonical command service continues to enforce acting-organization, tour/event collaborator access, lifecycle transitions, idempotent same-status behavior, and bulk target revalidation.
- The logistics site-map tent resource family now uses `withAdminCapability`: `site-maps/[id]/tents` GET under `logistics.view` and POST under `logistics.manage`; `tents/[tentId]` GET under `logistics.view` with PUT/DELETE under `logistics.manage`; every handler passes `requiredOrgId: admin.orgId` into `getSiteMapAccess` and keeps its access-level, validation, and activity-log behavior.
- `site-maps/[id]/publish-work-mode` is classified `capability_gated` with `site_map.edit` in the route registry (previously a code-vs-registry drift); the legacy baseline is exact at 100/100.
- Staff-operations channels (`channels`, `channels/[id]`) intentionally remain on the manual disjunctive gate: the registry records their `anyOf` policy (`communications.broadcast` OR `workforce.manage`), which the single-capability `withAdminCapability` wrapper cannot express without changing authorization.
- Organization settings GET now uses `org.settings.manage`; its acting-organization profile/settings queries and `canEdit` projection remain intact, with PATCH already using the same capability.
- ADMUX-0102 local navigation hardening now uses an explicitly modal Radix mobile Sheet with trigger focus restoration; collapsed controls remain named and internal destinations remain client-routed. Local audit/control-plane checks pass, but ADM-B13/ADMUX-0102 status is not promoted without governed immutable/external evidence.
- The logistics site-map task/layer/measurement/issue families (8 routes, 15 handlers) now use `withAdminCapability`: collection GET under `logistics.view`, all mutations (POST/PATCH/PUT/DELETE) under `logistics.manage`; every handler passes `requiredOrgId: admin.orgId` into `getSiteMapAccess` and preserves its `requireSiteMapAccess` read/edit/completeTask gates, the tasks POST action discriminator (ASSIGN_TASK → edit, others → completeTask), assignment-ownership checks, event-task fan-out, collaborator upserts, notifications, activity logging, and issue `resolved_at` transitions.
- The admin route registry is exact at 90/90 legacy: this batch classified `site-maps/[id]/tasks`, `tasks/[taskId]`, `layers`, `layers/[id]`, `measurements`, `measurements/[id]`, `issues`, and `issues/[id]` as `capability_gated` (logistics.view) and lowered the baseline `legacyRouteLimit` to 90.
- RBAC registry discrepancy RESOLVED with code evidence: `rbac/roles` (GET/POST), `rbac/roles/[id]` (DELETE), `rbac/assign-role` (POST), and `rbac/entity/[entityType]/[entityId]/assignments` + `/audit` (GET) are all on `withAdminCapability('org.roles.manage')` in the routed code (with the `has_entity_permission(..., 'MANAGE_MEMBERS')` entity boundary preserved); the registry now lists all five as `capability_gated (org.roles.manage)`, corroborating the earlier RBAC migration record.
- Tour team route reconciliation: registry now records `workforce.view` for `tours/team-members` and `tours/teams` (was `tour.manage`). With the view→manage derivation this matches code truth exactly: GET binds `workforce.view`, POST/PATCH/DELETE bind `workforce.manage`; `validateWorkforceAssignmentParents` and `assertAdminTourAccess` remain the tour/org data-boundary checks.
- Additional registry/code drifts synced to code truth with handler evidence: `tasks` (code `withAdminCapability('logistics.view'/'logistics.manage')` with `requireOpsOrgId` org boundary) now `capability_gated (logistics.view)`; `workforce/people` (code `workforce.view` with `admin.orgId` directory bound) now `capability_gated (workforce.view)`; `logistics/items/[id]/equipment` (code `logistics.manage` for both POST attach and DELETE detach plus `withLogisticsParentOrgId` parent-org resolution) now `capability_gated`; `vendor-requests` POST and `vendor-requests/[id]` PATCH (code `vendor.manage` with `assertAdminEventAccess`/`requireEventChildAccess` event-org boundaries) now `capability_gated (vendor.view → vendor.manage on mutation)`.
- The site-map bulk-assign handler (`site-maps/[id]/zones/bulk-assign` POST) is now on `withAdminCapability('logistics.manage')` with `getSiteMapAccess(..., { requiredOrgId: admin.orgId })` and the preserved `requireSiteMapAccess(access, 'edit')` gate; `bulkAssignTeamToZone` keeps its `.eq('site_map_id', siteMapId)` zone-write scope and the event-zone/staffing-count sync behavior is unchanged. Registry entry flipped to `capability_gated`.
- The admin route registry is exact at 79/79 legacy: this batch classified the five RBAC entries, `tasks`, `workforce/people`, `logistics/items/[id]/equipment`, `vendor-requests`, `vendor-requests/[id]`, and `zones/bulk-assign` (11 entries) as `capability_gated` and lowered the baseline `legacyRouteLimit` to 79.
- The 13 latent `--admin-*` color tokens in app/admin/globals.css (`--admin-primary`…`--admin-info`) were RETIRED on 2026-09-13 (handoff HF-DESIGN-030-ADMIN resolved, Option B): zero `var()`/`rgb(var())` consumers repo-wide, no in-flight adoption plans, zero Tailwind projections, and no boundary contract. `--admin-transition-normal` (0.3s) is retained as the file's only `:root` token — it is consumed by `.admin-metric-card`/`.admin-btn-futuristic`, which are live classes used by `app/admin/admin-layout-client.tsx` and `app/admin/utils/theme-utils.ts`. Admin surface colors now resolve from literal rgb() values in app/admin/globals.css rules, the global theme defaults, or the `--dashboard-*` shell palette; do not reintroduce an `--admin-*` palette without a consumer.
- `logistics/site-maps/import` remains DEFERRED and registry `legacy_pending_migration`: the handler authorizes via the `has_entity_permission` RPC (`EDIT_EVENT_LOGISTICS`/`EDIT_TOUR_LOGISTICS`) with no acting-org resolution, and the site map it creates binds `event_id` to the legacy `events` table — the canonical `getSiteMapAccess` org boundary resolves org via `event_v2_id`/`tour_id`, so imported resources are org-unresolvable by the wrapper model; it is also a multi-object bulk import with partial-failure continuation.
- `venues` and `venues/[id]` remain DEFERRED (registry `legacy_pending_migration`): `venue_profiles` is a shared directory with no venue→org linkage (no entity-to-org check); GET on `[id]` queries `events_v2` cross-org by `ilike venue_name`; PATCH on `[id]` is a bare `withAdminAuth` unscoped mutation. `venues` collection GET/POST already apply `vendor.view`/`vendor.manage` capability checks in code via the manual `authenticateApiRequest + resolveActingAdminContext + hasAdminCapability` idiom — a registry/code note, not a migration.
- No `app/api/admin/events/[eventId]/attendees/*` or check-ins sub-resource routes exist in the tree (verified by glob); the events family has no attendees/check-in legacy entries to migrate.
- ADMIN-003 exhaustive legacy-sweep review (2026-09-13) confirmed the remaining legacy dataset is exact at 75/75 with ZERO cleanly migratable handlers. Every one of the 75 `legacy_pending_migration` entries was verified against routed code: the events family, logistics site-map/task/item families, logistics/transport (already `withAdminCapability('logistics.view'/'logistics.manage')` matching the registry), and vendor-requests (POST + [id] PATCH on `vendor.manage` with event-org boundaries) are fully canonical; no code-vs-registry drift remains in the legacy set (i.e., no legacy entry is secretly already gated). The remaining 75 decompose into: venue-scoped hiring/staffing (applications ×3, job-postings ×2, staff/dashboard, staffing ×5 — `canReviewStaffingApplications`/`canManageVenueStaffing` = `hasEntityPermission('Venue','ASSIGN_EVENT_ROLES')`/`canManageHiring`, no org boundary); platform/global ops kill-switches (creator-* ×11, institutional, licensing, rights-admin, rights-intelligence — `requireApiUser` + platform `assertAdmin` + music feature-flag writes); marketplace platform surfaces (moderation, orders, orders/[id], payouts/[id]/retry — `profile.role === 'admin'` gate with global queues/ledgers); messaging participant/user surfaces (messages/list user-scoped, messages/threads unscoped service-role, messages/broadcast platform-wide); multi-capability anyOf gates (calendar GET/POST + calendar/export — `CALENDAR_ENTRY_CAPABILITIES` is a 5-capability disjunction the single-capability wrapper cannot express, same class as staff-operations/channels); mixed org-scoped-mutation + cross-org-read families (travel-coordination, lodging, rentals — GET helpers have no `org_id` predicate, and lodging provider/room-type/availability mutations and rentals mutations are unscoped raw `eq(id)`); global/unscoped aggregates and helpers (analytics/top-performers, dashboard/stats mixed aggregator, capabilities POST introspection, error-reporting, test, travel/flight-lookup external lookup); and the documented deferred families (communications, notifications POST, onboarding ×17, team-members mixed, logistics/equipment/catalog global catalog, logistics/site-maps/import, logistics/vendor/* vendorId-param-scoped with no vendor→org linkage, store user-scoped seller listings, venues + venues/[id]). The registry CI check passes unchanged at legacy 75/75 (289 route files, REL-103 contracts exact); no baseline flip was warranted because no legacy entry has gated code.
- Pre-existing observations not changed: `staffing/*` remain venue-scoped via `hasEntityPermission` with no org boundary and are deferred; top-level `app/api/admin/team-members` mutators use bare `authenticateApiRequest`/`checkAdminPermissions` and `team-members` GET (already `capability_gated (workforce.view)`) is not affected by this batch.
- The events communications registry/code drift is RESOLVED (2026-09-13, owner decision): `app/api/admin/events/[id]/communications` (GET/POST/PATCH) and `communication-settings` (GET/PATCH) are now wrapped with `withAdminCapability('event.view')` from `@/lib/auth/api-auth`, matching the registry's `capability_gated (event.view)` classification — a registry/code ALIGNMENT, not a new flip (registry entries unchanged, legacy baseline exact at 75/75). Every participant/event-owner membership check is preserved inside the handlers (event_participants, events_v2.created_by, employment_assignments worker checks, mark_read/acknowledge, pin/unpin/delete/moderate, settings owner gate). ACCEPTED RISK: the capability gate runs before membership checks, so non-admin participants/workers without an org admin capability are locked out of the participant-facing read/acknowledge surface — QA must verify; a participant-specific API surface or RBAC-aware read guard is the recommended follow-up if participant access must remain.
- Deferred ADMIN-003 candidates with reasons (communications family now migrated): `staffing/*` are venue-scoped via `hasEntityPermission` with no org boundary (shifts are multi-surface, mutations embed scheduling policy); top-level `app/api/admin/team-members` mutators use bare `authenticateApiRequest`/`checkAdminPermissions` without an org/venue boundary; `logistics/site-maps/import` authorizes via `has_entity_permission` RPC with no org boundary and creates org-unresolvable maps (legacy `events.id` binding); `venues`/`venues/[id]` are a shared venue directory with no venue→org linkage; participant access to event bulletins beyond admin capability grants is an accepted-risk gap needing a participant-specific surface (see resolved communications entry above).
- Marketplace moderation is a platform-global queue and now uses `withPlatformAdmin` for GET/PATCH. The legacy `profiles.role = 'admin'` check was removed, so organization/admin-surface membership cannot grant access; PATCH retains its queue-item `id` predicate and `assigned_admin_id = user.id` attribution. Its registry contract is `platform_admin` with a platform tenant target, and the exact legacy baseline is 74/74.
- The platform-global Marketplace order ledger now uses `withPlatformAdmin` for both collection and detail GET. The detail handler retains Next.js dynamic `params`, its `.eq('id', id)` resource predicate, and its 404 behavior; the collection keeps status/range query behavior. Both registry entries are `platform_admin`, and the exact legacy baseline is 72/72.
- The platform-global Marketplace payout-retry POST now uses `withPlatformAdmin`; the legacy role-string gate is removed. The handler retains dynamic `params`, payout-id predicates on both read and write, the `on_hold`/`failed`/`pending` eligibility allowlist, non-idempotent scheduled transition, three-day availability, incremented retry metadata, `lastRetryBy`/`lastRetryAt` actor attribution, and its existing 404/400/500 responses. Its registry contract is `platform_admin` and non-idempotent, with the exact legacy baseline at 71/71.
- The `app/api/admin/marketplace` family is fully classified: its complete on-disk inventory is moderation GET/PATCH, orders GET, orders/[id] GET, and payouts/[id]/retry POST. Every exported handler uses `withPlatformAdmin`, every registry entry is `platform_admin`, and no legacy role-string or unclassified Marketplace admin route remains. Future ADMIN-003 batches must select a different family rather than infer another Marketplace route.
- The platform-global rights-admin operations route (`rights-admin/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The music-rights-admin feature gate, open-deadline/flag reads, action schema, exact kill-switch flag-key predicate, non-idempotent mutation, and `music_rights_admin_audit_events` actor attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 70/70.
- The platform-global institutional operations route (`institutional/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The institutional feature gate, open-exception/flag reads, action schema, exact kill-switch flag-key predicate, non-idempotent mutation, 201/400/404/500 behavior after authorization, and `music_institutional_admin_actions.actor_user_id` attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 69/69.
- The platform-global licensing operations route (`licensing/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The licensing feature gate, open-conflict/flag reads, action schema/defaults, all ten kill-switch mappings, exact feature-flag key predicate, non-idempotent mutation, 200/400/404/500 behavior after authorization, and full `music_licensing_audit_events` actor/role/entity attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 68/68.
- The platform-global rights-intelligence operations route (`rights-intelligence/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The rights-intelligence feature gate, pending/failed outbox and flag read model, action schema/defaults, all individual kill switches, five-key `competition_stop`, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and `music_intelligence_audit_events` actor/action/metadata attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 67/67.
- The platform-global creator-cooperative operations route (`creator-cooperative/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The cooperative feature/capability gate, pending/failed outbox and multi-prefix flag read model, action schema/defaults, all kill switches, six-key `privacy_incident_stop`, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and `creator_cooperative_audit_events` actor/action/metadata attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 66/66.
- The platform-global creator-digital-commons operations route (`creator-digital-commons/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The digital-commons readiness gate, pending/failed outbox and prefix-filtered flag read model, action schema/defaults, all kill switches, four-key `tourify_exit_freeze`, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and complete `creator_commons_audit_events` actor/event/aggregate/payload/policy/idempotency fields remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 65/65.
- The platform-global creator-federation operations route (`creator-federation/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The federation admin-ops gate, pending/failed outbox and prefix-filtered flag read model, action schema/defaults, all kill switches, eight-key `federation_partition_stop`, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and `creator_federation_audit_events` actor/action/metadata attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 64/64.
- The platform-global creator-interoperability-convention operations route (`creator-interoperability-convention/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The convention-readiness gate, pending/failed outbox and prefix-filtered flag read model, action schema/defaults, all kill switches, five-key `convention_freeze`, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and full `creator_interop_audit_events` event/actor/subject/payload/hash attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 63/63.
- The platform-global creator-interoperability-institution operations route (`creator-interoperability-institution/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The institution-readiness gate, pending/failed outbox and prefix-filtered flag read model, action schema/defaults, all kill switches, eleven-key `institution_freeze`, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and full `creator_interop_institution_audit_events` event/actor/subject/payload/hash attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 62/62.
- The platform-global creator-interoperability-organization operations route (`creator-interoperability-organization/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. Despite the domain name, the route has no acting-organization or tenant selector: it operates on global outbox, flag, and audit resources. The organization-readiness gate, pending/failed outbox and prefix-filtered flag read model, action schema/defaults, all kill switches, nine-key `organization_freeze`, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and full `creator_interop_org_audit_events` event/actor/subject/payload/hash attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 61/61.
- The platform-global creator-protocol-constitution operations route (`creator-protocol-constitution/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The constitution-readiness gate, pending/failed outbox and prefix-filtered flag read model, action schema/defaults, all kill switches, `fundamental_provision_freeze`, `succession_crisis_stop`, and `emergency_sunset` mappings, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and full `creator_protocol_audit_events` event/actor/null-subject/payload/hash attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 60/60.
- The platform-global creator-public-infrastructure operations route (`creator-public-infrastructure/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The public-infrastructure admin-ops gate, pending/failed outbox and prefix-filtered flag read model, action schema/defaults, all kill switches, `identifier_abuse_stop`, five-key `trust_compromise_stop`, and `participation_withdrawal_broadcast` mappings, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and full `creator_public_audit_events` actor/event/object/payload attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 59/59.
- The platform-global creator-multilateral-treaty-operations route (`creator-multilateral-treaty-operations/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The treaty-operations readiness gate, pending/failed outbox and prefix-filtered flag read model, action schema/defaults, all seven action mappings, eleven-key `treaty_ops_freeze`, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and full `creator_treaty_ops_audit_events` event/actor/subject/payload/hash attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 58/58.
- The platform-global creator-treaty-system-renewal operations route (`creator-treaty-system-renewal/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The treaty-renewal readiness gate, pending/failed outbox and prefix-filtered flag read model, action schema/defaults, all six action mappings, nine-key `renewal_freeze`, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and full `creator_treaty_renewal_audit_events` event/actor/subject/payload/hash attribution remain unchanged. Its registry contract is `platform_admin`; POST is explicitly audit-required through `auditMethods`, and the exact legacy baseline is 57/57.
- The platform-global creator-treaty-system-legacy operations route (`creator-treaty-system-legacy/ops` GET/POST) now uses `withPlatformAdmin`, replacing its duplicated `requireApiUser` plus local profile assertion. The treaty-legacy readiness gate, pending/failed outbox and prefix-filtered flag read model, action schema/defaults, all five action mappings, thirteen-key `legacy_freeze`, exact per-key predicates, non-idempotent mutation, 200/400/404/500 behavior after authorization, and full `creator_treaty_legacy_audit_events` event/actor/subject/payload/hash attribution remain unchanged. The registry's former coarse `audit: true` was refined to code-exact POST-only audit metadata (`audit: false`, `auditMethods: ['POST']`), so GET is not falsely represented as emitting an audit event. Its registry contract is `platform_admin`, and the exact legacy baseline is 56/56.
- The complete on-disk creator admin operations inventory is now canonically guarded: eleven `app/api/admin/creator-*/ops/route.ts` files, each exporting GET/POST through `withPlatformAdmin`, with method-exact POST audit metadata and no remaining creator ops `legacy_pending_migration` entry.
- `analytics/top-performers` remains DEFERRED and `legacy_pending_migration`: it is the next remaining registry entry, but code does not prove whether it is intentionally platform-global or missing organization predicates. Its `withAdminAuth` compatibility gate admits platform admins, organization admins/owners, and confirmed tour collaborators, while the handler reads cross-organization `event_attendance`, `artist_profiles`, `profiles`, and `events_v2` without an acting-org, event-access, or other tenant predicate. Migrating it to `withPlatformAdmin` would tighten current organization/tour access without an explicit ownership decision; migrating it to an organization capability would expose an unscoped global aggregate. Required precondition: owner decision that the report is platform-only, or organization/event predicates plus an organization capability contract.
- The three-entry applications family (`applications`, `applications/[id]`, and `applications/[id]/audit`) remains DEFERRED and `legacy_pending_migration`. It is not platform-global: collection GET requires `venue_id` or resolves one from `job_posting_id`; collection POST rejects/evidence actions, bulk PATCH, and item PATCH load each application and authorize its venue; approval resolves the application employer and calls `assertCanManageHiring`; audit GET resolves a venue/organization/artist employer before `canManageHiring`. These grants include direct venue/entity ownership, `can_manage_hiring`, organization `hiring.manage`, and legacy entity permissions, and bulk operations may span venues. The canonical acting-organization wrapper cannot preserve those legitimate grants because there is no venue/employer→acting-org contract for every application. Required precondition: durable employer/venue→org resolution plus a policy decision mapping direct entity grants to an acting organization (including multi-venue bulk behavior); keep the existing per-resource checks after any future wrapper migration.

## Current focus

- ADMIN-003 remains active. The registry is exact at 56/56 legacy after canonically guarding the complete eleven-route creator operations inventory, the single-route rights-admin, institutional, licensing, and rights-intelligence operations families, and the complete `app/api/admin/marketplace` family. No creator ops route remains in `legacy_pending_migration`. The other recorded legacy families still require their tenant/resource preconditions or explicit platform classification. Next organization-scoped batches still wait on: a venue→org linkage (applications, job-postings, staffing, staff/dashboard), a vendor→org/equipment linkage (logistics/vendor/*, equipment/catalog), a single-capability calendar policy or an anyOf-capable wrapper (calendar, calendar/export), org predicates on travel/lodging/rentals reads (and lodging/rentals unscoped mutation actions), and org resolution for site-maps/import and venues. Communications (events/[id]/communications + communication-settings) were migrated 2026-09-13 onto `withAdminCapability('event.view')` per owner decision (accepted participant-access risk recorded); notifications POST, team-members mutators, onboarding, messages, and the remaining platform/global surfaces are not claimed complete.
- 2026-09-13 owner-decision migration batch: the events communications route family (2 routes, 5 handlers) is now on `withAdminCapability('event.view')`, closing the tracked registry/code drift as an alignment (baseline exact at 75/75). Focused Vitest 5 files / 83 tests passed; ESLint clean; registry CI check exact; scoped tsc zero diagnostics in changed files; agents:validate 17 agents / 99 tasks / 0 warnings / 0 errors.

## Precondition handoffs

ADMIN-003 is precondition-bound: the exhaustive legacy sweep (2026-09-13) found zero remaining legacy admin handlers that are migratable as-is. These are handshake notes for the owning lanes — durable docs recorded here and in ADMIN-003 `next_steps` so future ADMIN-003 migration batches reference them. Re-audit each family only after its referenced linkage/policy lands.

1. venue→org linkage — hiring/staffing families (`applications` ×3 + `[id]` PATCH + `[id]/audit`, `job-postings` ×2 + `[id]` GET/PATCH, `staff/dashboard` GET, `staffing` family ×5) are venue-scoped via `canReviewStaffingApplications`/`canManageVenueStaffing` = `hasEntityPermission('Venue', 'ASSIGN_EVENT_ROLES')`/`canManageHiring`, with no org boundary the `withAdminCapability` wrapper can bind. Owning lanes: **venue** + **database**.
2. vendor→org linkage — `logistics/vendor/dashboard|inventory|workflows` are vendorId-query-param scoped with no vendor→org linkage (dashboard POST has no handler auth) and `logistics/equipment/catalog` is a global catalog (`GET` has no org filter; POST comment "Equipment catalog is global"). Owning lanes: **venue/logistics owner** + **database**.
3. Calendar capability policy — `calendar` GET/POST and `calendar/export` gate on `CALENDAR_ENTRY_CAPABILITIES`, a 5-capability anyOf the single-capability `withAdminCapability` wrapper cannot express (same class as staff-operations/channels). Owning lanes: **admin capability-policy owner** (+ **database** if the policy moves to data).
4. org predicates on travel/lodging/rentals — `travel-coordination`, `lodging`, and `rentals` read helpers have no `org_id` predicate, and lodging provider/room-type/availability and rentals mutation actions are unscoped raw `eq(id)` writes. Owning lane: **database**.
5. org resolution for site-maps/import (v1 events linkage) and venues directory — `site-maps/import` creates maps bound to legacy `events.id`, org-unresolvable by the canonical `getSiteMapAccess` event_v2_id/tour_id boundary; `venue_profiles` is a shared directory with no venue→org linkage. Owning lanes: **database** + **venue**.

## Known risks

- The repository was already heavily modified at bootstrap.
- Generated maps describe topology, not behavioral correctness.
- The verified W01 changes are still uncommitted in a shared dirty worktree.
- The tour transition migration is a one-route batch; no other legacy handler was changed in this pass.

Update this file only when a task establishes a durable fact future work needs.

## Production launch graph — 2026-09-16

- ADMIN-003 is P0. Every production admin API must be classified and protected by the canonical platform or organization guard with explicit resource scope.
- Launch acceptance requires cross-role, cross-tenant, and cross-resource denial coverage plus zero undocumented exception routes; prior focused migration evidence does not satisfy the broadened gate.
- 2026-09-17 guard batch: event-discovery admin routes (`event-merges`, `event-claims`, `event-providers`, and `event-sync`) now use `withPlatformAdmin`, replacing duplicated `checkIsAdmin`/cookie checks while preserving feature flags and service-role data operations. Focused tests cover platform-role denial and cross-tenant acting-context mismatch; hosted evidence remains open.
- 2026-09-18 guard batch: Marketplace moderation GET/PATCH now use `withPlatformAdmin`; focused tests prove role-only profiles are denied, route adoption is canonical, and both method contracts are platform-targeted. Registry verification is exact at 74/74; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Marketplace orders collection/detail GET now use `withPlatformAdmin`; the detail wrapper closes over the awaited dynamic route parameter so the existing order-ID predicate is preserved. Focused platform-contract tests, lint, and registry verification pass at exact legacy 72/72; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Marketplace payout retry POST now uses `withPlatformAdmin`; direct route tests prove missing and ineligible resources cannot reach the mutation, while an eligible retry remains ID-scoped and records the platform actor. Focused tests, lint, registry verification, and control-plane validation pass at exact legacy 71/71; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Rights-admin operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled feature gate performs no trusted mutation and an enabled kill-switch remains key-scoped with actor-attributed audit data. Registry verification is exact at 70/70 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Institutional operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled feature gate performs no trusted mutation and an enabled kill-switch remains key-scoped with actor-attributed admin-action data. Registry verification is exact at 69/69 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Licensing operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled feature gate performs no trusted mutation and an enabled DDEX kill-switch remains key-scoped with the original actor/role/entity audit attribution. Registry verification is exact at 68/68 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Rights-intelligence operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled feature gate performs no trusted mutation, the enabled read keeps pending/failed outbox and flag projections, and `competition_stop` disables the exact five keys with actor-attributed audit metadata. Registry verification is exact at 67/67 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-cooperative operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled feature gate performs no trusted mutation, the enabled read keeps pending/failed outbox and multi-prefix flag projections, and `privacy_incident_stop` disables the exact six keys with actor-attributed audit metadata. Registry verification is exact at 66/66 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-digital-commons operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled readiness gate performs no trusted mutation, the enabled read keeps pending/failed outbox and prefix-filtered flag projections, and `tourify_exit_freeze` disables the exact four keys with the original actor/event/aggregate/policy/idempotency audit payload. Registry verification is exact at 65/65 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-federation operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled admin-ops gate performs no trusted mutation, the enabled read keeps pending/failed outbox and prefix-filtered flag projections, and `federation_partition_stop` disables the exact eight keys with actor-attributed audit metadata. Registry verification is exact at 64/64 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-interoperability-convention operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled readiness gate performs no trusted mutation, the enabled read keeps pending/failed outbox and prefix-filtered flag projections, and `convention_freeze` disables the exact five keys with the original event/actor/subject/payload/hash audit fields and schema defaults. Registry verification is exact at 63/63 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-interoperability-institution operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled readiness gate performs no trusted mutation, the enabled read keeps pending/failed outbox and prefix-filtered flag projections, and `institution_freeze` disables the exact eleven keys with the original event/actor/subject/payload/hash audit fields and schema defaults. Registry verification is exact at 62/62 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-interoperability-organization operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled readiness gate performs no trusted mutation, the enabled read keeps pending/failed outbox and prefix-filtered flag projections, and `organization_freeze` disables the exact nine keys with the original event/actor/subject/payload/hash audit fields and schema defaults. Registry verification is exact at 61/61 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-protocol-constitution operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled readiness gate performs no trusted mutation, the enabled read keeps pending/failed outbox and prefix-filtered flag projections, and `fundamental_provision_freeze` disables its exact three keys with the original event/actor/null-subject/payload/hash audit fields and schema defaults. Registry verification is exact at 60/60 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-public-infrastructure operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled admin-ops gate performs no trusted mutation, the enabled read keeps pending/failed outbox and prefix-filtered flag projections, and `trust_compromise_stop` disables the exact five keys with the original actor/event/object/payload audit fields and schema defaults. Registry verification is exact at 59/59 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-multilateral-treaty-operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled readiness gate performs no trusted mutation, the enabled read keeps pending/failed outbox and prefix-filtered flag projections, and `treaty_ops_freeze` disables the exact eleven keys with the original event/actor/subject/payload/hash audit fields and schema defaults. Registry verification is exact at 58/58 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-treaty-system-renewal operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled readiness gate performs no trusted mutation, the enabled read keeps pending/failed outbox and prefix-filtered flag projections, and `renewal_freeze` disables the exact nine keys with the original event/actor/subject/payload/hash audit fields and schema defaults. Registry verification is exact at 57/57 with POST audit-required; hosted evidence and release verification remain open.
- 2026-09-18 guard batch: Creator-treaty-system-legacy operations GET/POST now use `withPlatformAdmin`; focused tests prove the disabled readiness gate performs no trusted mutation, the enabled read keeps pending/failed outbox and prefix-filtered flag projections without emitting an audit event, and `legacy_freeze` disables the exact thirteen keys with the original event/actor/subject/payload/hash audit fields and schema defaults. Registry audit metadata is now method-exact (GET not-applicable, POST required), and verification is exact at 56/56; hosted evidence and release verification remain open.
- 2026-09-18 bounded registry review: the first five legacy families after `applications` have no proven platform-admin boundary, so no product or registry change was made. Calendar requires an acting organization plus its five-capability source-access policy and tour/event predicates; `capabilities` is signed-user/entity permission introspection; communications GET/POST are organization-scoped while PATCH still needs a message-participant/resource predicate; dashboard stats is organization/user scoped despite two global directory counts; error-reporting performs no durable platform operation and its compatibility-admin audience is not evidence of platform-only ownership. Preconditions are respectively an anyOf-capable organization guard, a canonical entity/self-service contract, message participant/organization scoping with method-level guards, removal or explicit treatment of global counts within the tenant dashboard, and an owner decision on the error-reporting audience. Registry verification remains exact at 56/56; hosted evidence and release verification remain open.
- 2026-09-18 bounded registry review after `error-reporting`: none of the next five legacy families has proven platform-admin ownership, so no product or registry change was made. Job postings require durable venue→org resolution plus per-posting venue checks; lodging requires organization predicates across reads and every provider/room/availability mutation; the global equipment catalog currently admits any authenticated user and needs an explicit owner/access decision plus vendor attribution; site-map import needs correct RPC result evaluation and canonical event/tour→org resolution before its partial multi-object write; vendor dashboard/inventory/workflows need vendor→org ownership and target-vendor predicates, including authentication for dashboard POST. Registry verification remains exact at 56/56; hosted evidence and release verification remain open.
