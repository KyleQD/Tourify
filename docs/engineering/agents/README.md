# Agent domains

`registry.yaml` is the routing index. Each domain has a charter, state, architecture, backlog, interfaces, decisions, verification guide, and working-set manifest.

No agent owns the whole repository. The orchestrator routes tasks and resolves overlap; it does not re-audit every domain.
