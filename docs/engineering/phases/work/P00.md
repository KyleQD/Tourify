# WORK-P00 — Engineering Baseline and Reconciliation Foundation

## Objective

Finish the verified Work-domain baseline so later implementation sessions do not need to repeat a full repository audit.

## Canonical task source

Implement and update only the `WORK-P00-*` records in:

`docs/engineering/execution/work.json`

## Already complete

- Engineering Constitution created.
- Current Work baseline anchored to `main` commit `76d8389ebf939cee70f7070abf74a6bacc46f5de`.
- Reference branch `backup/pre-reconcile-local-work-2026-08-20` verified and indexed.
- Current/reference `use-work-mode` and shared type differences recorded.
- Reference Work Hub dashboard and task semantics indexed.

## Remaining scope

### WORK-P00-T04 — Schema/RLS audit

Inspect only Work-related current schema and authorization surfaces. Record current tables, columns, policies, RPCs, constraints, and ownership boundaries. Resolve historical table proposals to existing equivalents when possible. Do not author product migrations during this task.

### WORK-P00-T05 — Current dependency index

Locate exact current paths for Work routes/APIs/services plus messaging, notifications, documents, tours, events, jobs, staffing, task infrastructure, and worker operations that Work Hub should compose.

### WORK-P00-T06 — Baseline close

Update `baselines/work.json`, `reference-maps/work-reference-map.json`, and `decisions/DECISIONS.md` with verified results. Change baseline confidence only when the implementation scope is adequately verified.

## Required context

Load only:

1. `docs/engineering/ENGINEERING_CONSTITUTION.md`
2. `docs/engineering/baselines/work.json`
3. `docs/engineering/reference-maps/work-reference-map.json`
4. `WORK-P00-*` tasks from `docs/engineering/execution/work.json`
5. Work-related current schema/API/service files encountered during the audit

Historical Work planning documents are optional supporting evidence; do not read them all by default.

## Gate

- JSON files remain valid machine-readable JSON.
- `npm run audit:worktree`
- `npm run typecheck` when code/schema inspection or supporting scripts are modified.
- No destructive migration or product implementation is included in this phase.

## Complete when

`WORK-P00-T01` through `WORK-P00-T06` are `verified`, unresolved canonical ownership conflicts are recorded or resolved, and WORK-P01 can execute without another full Work-domain audit.