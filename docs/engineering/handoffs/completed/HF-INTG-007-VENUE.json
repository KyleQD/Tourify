{
  "schema_version": "1.0",
  "id": "HF-INTG-007-VENUE",
  "task_id": "INTG-007",
  "from_agent": "integrations",
  "to_agent": "venue",
  "status": "completed",
  "created_at": "2026-09-21T15:05:00.000Z",
  "resolved_at": "2026-09-22T00:00:00.000Z",
  "source_sha": "b93967752b4262a2d7441755843eb886514fef26",
  "summary": "INTG-007 retires the venue legacy plaintext credential path. lib/integrations/token-vault.ts no longer dual-writes access_token/refresh_token into venue_social_integrations and no longer falls back to legacy columns on read (fail-closed); the authored-but-unapplied migration 20260921000000 nulls the legacy venue plaintext columns and the key_version=0 plaintext vault bytes. app/api/venue/integrations/route.ts still SELECTs legacy refresh_token and uses it as a refresh fallback when the vault has none; after these changes that fallback is always null (fail-closed 400), so the route should drop the legacy select/fallback and read the vault only. RESOLUTION (venue agent, 2026-09-22): POST now selects `id, venue_id, platform` only, drops the row.refresh_token fallback, relies exclusively on readVenueIntegrationSecrets().refreshToken (fails closed), and disconnect no longer writes the retired legacy plaintext columns; GET health semantics unchanged (needs_reauth when the vault token is missing). Focused venue test __tests__/venue/venue-integrations-api.test.ts (9 tests) proves the encrypted-envelope-only path and intentional failure behavior. Venue lane is clear for the database lane to apply migration 20260921000000.",
  "working_set": [
    "app/api/venue/integrations/route.ts"
  ],
  "completed": [
    "Vault write path is now encrypted-only (writeVenueIntegrationSecrets never touches venue_social_integrations legacy columns).",
    "Vault read path is fail-closed: readVenueIntegrationSecrets returns null for legacy/undecryptable material and never falls back to venue_social_integrations.access_token/refresh_token.",
    "Additive migration authored (unapplied, CP-051) that nulls legacy venue plaintext columns and key_version=0 vault bytes once the venue route no longer reads them.",
    "Venue lane cleanup executed 2026-09-22: removed the legacy refresh_token column select and the row.refresh_token fallback in POST refresh; refresh now reads the encrypted vault only and fails closed with an intentional 400 when the vault holds no refresh grant.",
    "POST disconnect no longer writes access_token/refresh_token nulls to the retired legacy columns — vault deletion + is_connected toggle only.",
    "GET health semantics confirmed unchanged: health derives from row is_connected plus vault accessToken presence (connected / needs_reauth / disconnected); GET select remains 'id, platform, account_handle, is_connected, last_sync' (no token columns).",
    "Focused venue test suite added (__tests__/venue/venue-integrations-api.test.ts, 9 tests) asserting the encrypted-envelope-only path, vault-derived health, vault-only disconnect, fail-closed refresh 400, and a route-source guard that no legacy plaintext column reference remains."
  ],
  "next_steps": [
    "Database lane: apply migration 20260921000000 to isolated staging per CP-051 — venue lane no longer reads the legacy venue plaintext columns, so nulling them is safe.",
    "Admin and social lanes complete their INTG-007 handoffs before the migration applies.",
    "RELEASE-008 consumes the credential-vault evidence before any provider enablement."
  ],
  "blockers": [
    "Hosted migration apply is out of band (CP-051); venue refresh for re-connected gated providers remains disabled through RELEASE-008."
  ],
  "verification": [
    "npx vitest run __tests__/venue/venue-integrations-api.test.ts — 9/9 passed (GET vault-derived health x3, disconnect vault-only, refresh envelope-only success, fail-closed 400, 404 owner mismatch, unknown platform 400, route-source plaintext guard).",
    "npx vitest run __tests__/venue __tests__/integrations — 16 files / 99 tests passed.",
    "rg 'refresh_token|access_token' app/api/venue lib/venue — only OAuth provider-protocol uses remain (grant endpoint URLs, grant_type/payload fields, writeVenueIntegrationSecrets args); lib/venue has zero matches.",
    "Scoped typecheck: npx tsc -p tsconfig.scoped-check.json --noEmit --incremental false (route + new test) — exit 0.",
    "npx eslint app/api/venue/integrations/route.ts __tests__/venue/venue-integrations-api.test.ts — exit 0.",
    "npm run agents:validate — 0 errors (see INTG-007 task record for exact output)."
  ]
}