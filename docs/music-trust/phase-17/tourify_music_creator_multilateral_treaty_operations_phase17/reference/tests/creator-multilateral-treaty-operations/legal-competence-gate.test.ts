import {describe,it,expect} from 'vitest';import{evaluateCompetence}from '../../lib/music/creator-multilateral-treaty-operations/legal-competence-gate';describe('competence',()=>{it('defaults deny outside effective powers',()=>expect(evaluateCompetence({requestedPower:'x',effectivePowers:[],amendmentEffective:true,participantApprovalComplete:true,suspended:false}).allowed).toBe(false));});
