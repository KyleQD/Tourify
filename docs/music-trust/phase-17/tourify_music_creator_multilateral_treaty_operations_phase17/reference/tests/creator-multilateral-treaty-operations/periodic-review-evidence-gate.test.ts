import{describe,it,expect}from'vitest';import{evaluateReviewEvidence}from'../../lib/music/creator-multilateral-treaty-operations/periodic-review-evidence-gate';describe('evidence',()=>{it('blocks stale evidence',()=>expect(evaluateReviewEvidence({manifestVerified:true,methodologyApproved:true,minimumCoverageMet:true,conflictsDisclosed:true,confidentialDataMinimized:true,stale:true}).allowed).toBe(false));});
