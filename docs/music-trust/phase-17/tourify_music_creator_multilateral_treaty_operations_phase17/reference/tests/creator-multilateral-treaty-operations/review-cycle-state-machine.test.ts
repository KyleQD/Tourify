import { describe,it,expect } from 'vitest'; import { mayTransitionReview } from '../../lib/music/creator-multilateral-treaty-operations/review-cycle-state-machine'; describe('review cycle',()=>{it('allows evidence collection after mandate',()=>expect(mayTransitionReview('mandate_approved','evidence_collection')).toBe(true));it('blocks direct planned to conference',()=>expect(mayTransitionReview('planned','conference')).toBe(false));});
