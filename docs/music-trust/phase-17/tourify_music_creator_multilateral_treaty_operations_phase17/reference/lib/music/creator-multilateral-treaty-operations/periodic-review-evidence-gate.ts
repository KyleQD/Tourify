export interface ReviewEvidenceInput { manifestVerified:boolean; methodologyApproved:boolean; minimumCoverageMet:boolean; conflictsDisclosed:boolean; confidentialDataMinimized:boolean; stale:boolean; }
export function evaluateReviewEvidence(i:ReviewEvidenceInput){ const blockers:string[]=[]; if(!i.manifestVerified)blockers.push('manifest'); if(!i.methodologyApproved)blockers.push('methodology'); if(!i.minimumCoverageMet)blockers.push('coverage'); if(!i.conflictsDisclosed)blockers.push('conflicts'); if(!i.confidentialDataMinimized)blockers.push('minimization'); if(i.stale)blockers.push('stale'); return {allowed:blockers.length===0,blockers}; }
