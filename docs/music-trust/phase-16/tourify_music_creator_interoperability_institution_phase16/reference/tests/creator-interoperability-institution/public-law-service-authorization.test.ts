import { describe, expect, it } from 'vitest'; import { authorizePublicLawService } from '../../lib/music/creator-interoperability-institution/public-law-service-authorization';
describe('service authorization',()=>{it('denies stale source',()=>{const result=authorizePublicLawService({service:{id:'s',state:'effective',policyVersion:'1',schemaVersion:'1',jurisdiction:'US',effectiveAt:'2026',expiresAt:null,sourceManifestId:'m',auditEventId:'a',code:'x',legalBasisId:'l',allowedParticipantClasses:['state'],allowedJurisdictions:['US'],highImpact:true,nonAdjudicative:true},authority:null,jurisdiction:'US',localReservedPowerConflict:false,sourceCurrent:false});expect(result.allowed).toBe(false);});});
