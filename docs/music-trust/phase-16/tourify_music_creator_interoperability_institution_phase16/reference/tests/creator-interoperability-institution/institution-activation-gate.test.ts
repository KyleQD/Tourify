import { describe, expect, it } from 'vitest'; import { evaluateInstitutionActivation } from '../../lib/music/creator-interoperability-institution/institution-activation-gate';
describe('activation',()=>{it('requires two operators',()=>{expect(evaluateInstitutionActivation({legalBasisEffective:true,participantAuthorityVerified:true,organsOperational:true,hostReady:true,fundingApproved:true,oversightOperational:true,staffRemedyAvailable:true,privacyApproved:true,securityApproved:true,accessibilityApproved:true,competitionApproved:true,independentImplementations:2,independentOperators:1,tourifyUnavailableTestPassed:true,unresolvedCriticalBlockers:0}).allowed).toBe(false);});});
