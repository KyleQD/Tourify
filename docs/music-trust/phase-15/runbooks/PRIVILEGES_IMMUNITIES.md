# Runbook: Privileges / Immunities

Privileges remain hard-disabled. If any attempt to assert immunities:

1. Confirm `creator_interop_org_privileges_enabled` forced false in resolver.
2. Admin ops → `privileges_stop`.
3. Privilege schedules must remain `not_applicable` without host instrument + waiver + remedy.
4. Escalate to counsel; do not grant via feature flag alone.
