-- REFERENCE OUTLINE ONLY.
create table if not exists public.creator_memory_public_finding_aids (id uuid primary key default gen_random_uuid(), source_type text not null, source_id uuid not null, public_payload jsonb not null, source_version text not null, freshness_at timestamptz not null, restriction_status text not null, dispute_status text not null, projection_version text not null, revoked_at timestamptz, created_at timestamptz not null default now());
create table if not exists public.creator_memory_incidents (id uuid primary key default gen_random_uuid(), incident_type text not null, severity text not null, state text not null default 'open', affected_scope jsonb not null, public_notice jsonb, opened_at timestamptz not null default now(), closed_at timestamptz);
create table if not exists public.creator_memory_audit_events (id uuid primary key default gen_random_uuid(), actor_id uuid, actor_authority_id uuid, action text not null, target_type text not null, target_id uuid, policy_version text not null, schema_version text not null, source_manifest_id uuid, event_payload jsonb not null, occurred_at timestamptz not null default now());
create table if not exists public.creator_memory_outbox (id uuid primary key default gen_random_uuid(), event_type text not null, aggregate_type text not null, aggregate_id uuid not null, payload jsonb not null, idempotency_key text not null unique, state text not null default 'pending', attempt_count integer not null default 0, available_at timestamptz not null default now(), created_at timestamptz not null default now());
alter table public.creator_memory_public_finding_aids enable row level security; alter table public.creator_memory_incidents enable row level security; alter table public.creator_memory_audit_events enable row level security; alter table public.creator_memory_outbox enable row level security;
-- Public access should be through a reviewed security-invoker view or route, not direct table grants.
