-- REFERENCE OUTLINE ONLY. Create a real additive migration after auditing the deployed schema.
create table if not exists public.creator_memory_trusts (id uuid primary key default gen_random_uuid(), legal_name text not null, legal_character text not null default 'proposed', jurisdiction text not null, state text not null default 'draft', charter_version text, effective_at timestamptz, expires_at timestamptz, policy_version text not null, schema_version text not null, source_manifest_id uuid, created_at timestamptz not null default now());
create table if not exists public.creator_memory_trust_participations (id uuid primary key default gen_random_uuid(), trust_id uuid not null references public.creator_memory_trusts(id), participant_user_id uuid, participant_org_id uuid, state text not null default 'draft', joined_at timestamptz, withdrawn_at timestamptz, idempotency_key text not null unique, created_at timestamptz not null default now());
create table if not exists public.creator_memory_cultural_authorities (id uuid primary key default gen_random_uuid(), trust_id uuid not null references public.creator_memory_trusts(id), authority_name text not null, scope jsonb not null default '[]'::jsonb, territories jsonb not null default '[]'::jsonb, state text not null default 'under_review', evidence_manifest_id uuid, effective_at timestamptz, expires_at timestamptz, disputed_at timestamptz, created_at timestamptz not null default now());
alter table public.creator_memory_trusts enable row level security; alter table public.creator_memory_trust_participations enable row level security; alter table public.creator_memory_cultural_authorities enable row level security;
-- Add explicit default-deny policies only after actor/authority helper functions are audited.
