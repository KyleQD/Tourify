# Runbook: Approval Package

1. Approval packages live in `future_phase14_approval_packages`.
2. Execution via API remains 403 until counsel dual-control package.
3. State/IO participation requires `state_or_io_package_attached=true` and separate legal package.
4. Never treat Phase 13 flag enablement as Phase 14 approval.
