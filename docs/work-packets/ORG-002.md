# Work packet: `ORG-002`

## Goal

- Goal: Reconcile organization tenant, public/ops profile, and account projection identities into one canonical runtime contract.
- Out of scope: SQL migrations, schema changes, production changes, broad route migration, and account-service/UI rewrites.
- Owner/status: `organization` / `completed`

## Context

- Affected subsystem: organization tenant context and auth acting-context resolution.
- Routes/components/services: `lib/organizations/identity.ts`, `lib/auth/admin-context.ts`, `lib/auth/acting-context.ts`.
- References to read first: `docs/engineering/INDEX.md`, organization/general-user charters and states, `docs/engineering/agents/organization/QUESTIONS.md`, `docs/engineering/DECISIONS.md`, Supabase skill guidance.
- Known constraints: Preserve existing RLS and authorization checks; do not reset/replay migrations; preserve unrelated dirty worktree changes.

## Checklist

- [x] Confirm the current identity behavior from migrations, RLS, and scoped auth code.
- [x] Implement the smallest scoped canonical identity contract.
- [x] Route scoped auth context resolution through the contract.
- [x] Add focused identity contract tests.
- [x] Run focused tests, lint, and typecheck.
- [x] Record the decision and remaining migration/data-repair follow-up.

## Acceptance criteria

- [x] One canonical organization identity model.

## Verification

- Tier: `feature`
- Commands:
  - `npx vitest run __tests__/organization/org-identity-model.test.ts __tests__/organization/org-invite-accept-contract.test.ts __tests__/organization/org-personas-integration.test.ts lib/auth/__tests__/normalize-account-type.test.ts`
  - `npx eslint lib/organizations/identity.ts lib/auth/admin-context.ts lib/auth/acting-context.ts __tests__/organization/org-identity-model.test.ts`
  - `npm run typecheck`
  - `npm run agents:generate`
  - `npm run agents:validate`
- Evidence: Focused tests passed (4 files / 26 tests); scoped ESLint passed; repository typecheck passed; topology/control-plane checks recorded in ORG-002 task evidence.

## Handoff

- Changed areas: shared organization identity contract, admin/general acting-context consumers, focused tests, organization decision/state/architecture records.
- Failures and pre-existing failures: none observed in the selected checks.
- Blockers: No code blocker. Data repair or retirement of legacy projection rows remains a separately sequenced database task.
- Next action: Use `organizationId` for new tenant-scoped features; do not introduce new authorization checks against `accounts` or unbridged organizer profiles.
