# Work packet: `MKT-003`

## Goal

- Goal: Establish the Marketplace side of the CP-013 music commerce split for checkout, orders, transfers, portfolios, and related financial operations.
- Out of scope: Route-handler edits under `app/api/**`, Music catalog/rights/royalties, migrations, admin, venue, design, search, onboarding, feed, artist, release, and QA surfaces.
- Owner/status: `marketplace` / `active`

## Context

- Affected subsystem: `lib/marketplace/` commerce boundary and server auth adapter.
- Routes/components/services: Existing `/api/marketplace/checkout`, `/api/marketplace/orders`, and `/api/music-marketplace/**` are classified for coordinated route adoption; handlers are intentionally unchanged in this packet.
- References to read first: `docs/engineering/INDEX.md`, Marketplace charter/state, `docs/engineering/tasks/active/MKT-003.json`, `docs/DEVELOPMENT_WORKFLOW.md`, `docs/engineering/agents/marketplace/QUESTIONS.md`, `docs/engineering/DECISIONS.md`, and `MUSIC-003`'s Music-side boundary.
- Known constraints: Preserve unrelated dirty changes. Do not run migration resets or commit. Active marketplace schema remains ahead of migrations.

## Checklist

- [x] Confirm the current Marketplace/Music ownership and auth boundary
- [x] Implement the smallest scoped change
- [x] Add focused tests
- [x] Run the selected verification tier
- [x] Record failures and remaining blockers
- [x] State the next task

## Acceptance criteria

- [x] Marketplace owns checkout, orders, transfers, portfolios, and music-marketplace operations in a typed route boundary.
- [x] Marketplace exposes a server-only account gate backed by verified auth and server-side acting-context ownership checks.
- [x] Existing guest checkout behavior is documented as an explicit exception; authenticated order and music-marketplace routes have a required-auth contract ready for adoption.
- [ ] Existing route handlers adopt the boundary contracts; this requires a follow-up route batch outside this packet's allowed paths.

## Verification

- Tier: `fast`
- Commands: `npx vitest run lib/marketplace/__tests__/music-commerce-boundary.test.ts`; `npx eslint lib/marketplace/music-commerce-boundary.ts lib/marketplace/music-commerce-auth.ts lib/marketplace/__tests__/music-commerce-boundary.test.ts`; `npm run agents:validate`; `npx tsc --noEmit --pretty false --incremental false`
- Evidence: Marketplace boundary suite passed (7/7); all existing `lib/marketplace` unit suites passed (12 files, 133 tests); changed-file ESLint clean; `agents:validate` passed with the existing RELEASE-001 warning; project TypeScript check OOMed before diagnostics.

## Handoff

- Changed areas: Marketplace music-commerce boundary, Marketplace account auth adapter, focused tests, and this packet.
- Failures and pre-existing failures: Project-wide TypeScript check is resource-blocked by heap OOM before diagnostics; unrelated `git diff --check` findings are pre-existing dirty-worktree whitespace in admin/venue/database files.
- Blockers: Route adoption remains outside the explicit `lib/marketplace/` write boundary; active schema reconciliation remains owned by DB work.
- Next action: Adopt `requireMarketplaceAccount` in `/api/marketplace/orders` and `/api/music-marketplace/**`, while preserving guest checkout's explicit optional-auth path.
