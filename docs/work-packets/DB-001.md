# Work packet: `DB-001`

## Goal

- Goal: Audit the Database domain and leave a durable baseline, triaged gaps,
  owner questions, and verification evidence.
- Out of scope: production code, migration SQL, migration application, resets,
  task-model/registry changes, and other agents' surfaces.
- Owner/status: `database` / `completed`

## Context

- Affected subsystem: Supabase migrations, schema evolution, RLS, RPCs,
  generated types, data integrity, and their documented consumers.
- Routes/components/services: inventory only via
  `docs/engineering/generated/routes.md`, `api-routes.md`, `components.md`,
  `permissions.md`, and `integrations.md`.
- References to read first: `docs/engineering/INDEX.md`,
  `docs/DEVELOPMENT_WORKFLOW.md`, database `CHARTER.md`, `STATE.md`,
  `WORKING_SET.json`, `docs/DEVELOPMENT_BACKLOG.md`, and the database task
  record. The bounded packet was generated with
  `npm run agents:context -- --task DB-001`.
- Known constraints: preserve dirty worktree; active migrations are the source
  of truth; no reset/full-chain replay; generated maps are indexes, not proof.

## Checklist

- [x] Confirmed current source chain, validation manifests, RLS/RPC/type evidence,
  tests, and legacy work packets.
- [x] Reconciled the initial audit against DB-002/003/004/007 evidence.
- [x] Recorded the only scope expansion: historical migration archives,
  root-level SQL, and legacy `.agents` ledgers were read because the initial
  audit explicitly cited them as evidence for chain and schema drift.
- [x] Produced baseline, gap triage, owner questions, state, and backlog updates.
- [x] Ran the selected verification tier and recorded the CLI environment
  limitation for the generated-type check.

## Acceptance criteria

- [x] Durable baseline with path-backed claims and intended direction.
- [x] Every open gap triaged and evidenced.
- [x] Prioritized owner questions mapped to follow-up work.
- [x] No production or migration SQL changes made by DB-001.
- [x] `npm run agents:validate` passes with 0 errors.

## Verification

- Tier: `fast`
- Commands:
  - `npm run agents:context -- --task DB-001`
  - `npm run check:migration-chain`
  - `npm run check:migration-validation`
  - `MIGRATION_CHECKSUM_BASE_SHA=7cf660ad8422dbd3adbdb77369d94638cdc2231b npm run check:migration-checksums`
  - `npm run check:database-types` (environment-limited)
  - `npm run agents:validate`
- Evidence:
  - Migration chain: pass, 289 files, no duplicate policy creations.
  - Migration validation: pass, 142 source/history inputs; 106 manifests are
    99 planned, 2 isolated_validated, 5 staging_validated, 0 production_verified.
  - Migration checksums: pass, no changed migration SQL files.
  - Database type check: Supabase CLI could not write its telemetry temp file
    under `~/.supabase`; prior successful DB-004/DB-007 drift/type checks remain
    recorded in their completed task files.
  - Agent validation: pass, 0 errors and one pre-existing RELEASE-001 warning.

## Handoff

- Changed areas: `docs/engineering/agents/database/BASELINE.md`, `GAPS.md`,
  `QUESTIONS.md`, `STATE.md`, `BACKLOG.md`, and this packet.
- Failures and pre-existing failures: only the sandbox-blocked Supabase CLI
  telemetry write; no migration-chain or validation failures.
- Blockers: owner answers are intentionally deferred to follow-up tasks; the
  audit itself is not blocked.
- Next action: answer Q1–Q4 first, then create bounded follow-ups for RLS,
  ticketing/events, triggers, scale, erasure, and legacy cleanup.
