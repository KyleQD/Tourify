Tourify hiring / onboarding / comms — smoke checklist and audit notes
======================================================================

E2E smoke (artist opportunity board)
------------------------------------
1. GET /api/artist-jobs/categories — returns rows; category ids match UI selects.
2. POST /api/artist-jobs (logged in) — creates row; 400 with clear message if category invalid.
3. GET /api/artist-jobs — new job appears when status=open.
4. POST /api/artist-jobs/{id}/applications (as another user) — row in artist_job_applications.
5. GET /api/artist-jobs/{id}/applications (as job owner) — lists applications.
6. PATCH application status (owner): pending → reviewed → accepted; applicant notification if configured.

E2E smoke (venue staff board — separate product)
------------------------------------------------
1. POST job_posting path with venue context (admin job-postings or staffing action) — RBAC + insert.
2. Applicant flow against job_applications — approval blocked until hiring gate satisfied:
   verified staff_documents, agreement_acceptances, optional certifications per migration
   supabase/migrations/20260409183000_hiring_eligibility_gate.sql

Onboarding (docs vs code)
-------------------------
Entry: app/onboarding/page.tsx (query: type, token, invitation).
Flows: QuickSignupOnboarding; ArtistVenueOnboarding (artist | venue); StaffOnboarding; InvitationOnboarding.
Service: lib/services/unified-onboarding.service.ts
Docs: docs/STREAMLINED_ONBOARDING_FLOW.md, docs/ONBOARDING_IMPLEMENTATION_GUIDE.md, docs/ONBOARDING_SYSTEM_AUDIT_REPORT.md
Gap to watch: docs reference migrations/0004_unified_onboarding_schema.sql — confirm table names match current supabase/migrations.

Communications
--------------
Artist application PATCH: app/api/artist-jobs/[id]/applications/route.ts writes hiring_audit_events, notifications; OptimizedNotificationService for applicant.
Broader messaging schema: supabase/migrations/20260413210000_event_communications_system.sql
Verify RLS on notifications allows authenticated inserts used by that route (failures are caught and logged as warnings).

Database drift
--------------
Apply migration 20260423190222_ensure_artist_job_categories_seed.sql on environments missing category rows (fixes FK failures on job POST).

RLS reminder
------------
artist_job_applications INSERT requires auth.uid() = applicant_id — server routes must use createClient() session (fixed by passing supabase into ArtistJobsService.applyToJob).

Rebuild — unified facade + command center (Phase 2 / 8 / 10)
-----------------------------------------------------------
1. GET /api/jobs — returns artist_jobs (open) + optional staff_postings when venue_id query param set (session cookie).
2. POST /api/jobs — expect 405 with guidance until unified write is implemented.
3. Venue /venue/staff → Communications tab — loads team_communications for current venue; send inserts a row.
4. Admin /admin/dashboard/events/{id}/command-center — loads GET /api/events/{id}/hq; links to HQ, jobs facade, venue staff.
5. lib/rebuild/hiring-automation.ts — placeholder for approval→onboarding side effects (Phase 9).

Phases 3–8 (worker UI → command center)
---------------------------------------
1. GET /api/jobs?merge=1 — unified[] merges artist + venue; staffing tab uses this.
2. GET /api/me/applications — artist + venue application rows for logged-in user.
3. /jobs/{id}?source=artist|venue — detail + apply uses correct API per source.
4. POST /api/events/{eventId}/job-postings — creates job_posting_templates with event_id (ASSIGN_EVENT_ROLES).
5. /venue/dashboard/hiring-kanban — lists /api/admin/applications?venue_id=; PATCH status via /api/admin/applications/{id}.
6. /venue/staff scheduler tab — VenueStaffShiftsPanel + GET/POST /api/admin/staffing/shifts.
7. CLI: npm run test:hiring-shifts-smoke — GET /api/jobs?merge=1; optional HIRING_SMOKE_COOKIE + HIRING_SMOKE_VENUE_ID for shifts GET.
7. Event HQ — tab=chats loads EventChatsPanel → /api/admin/events/{id}/group-chats.
8. GET /api/events/{id}/hq — includes hiring, group_chats, documents; command center shows counts.
9. RBAC: GET /api/me/applications as other user must not return another user’s rows (RLS).
10. lib/services/unified-documents.service.ts — listVisibleEventDocuments mirrors HQ visible_to filtering.
